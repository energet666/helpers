# 🛠 Helpers

Коллекция скриптов для быстрой настройки серверов.

## setup.sh — Первоначальная настройка VPS

Автоматическая установка и настройка окружения на свежем Debian/Ubuntu сервере.

### Что устанавливается

| Категория | Компоненты |
|---|---|
| **Shell** | Zsh, Oh-My-Zsh (autosuggestions, syntax-highlighting, z) |
| **Терминал** | Tmux + TPM (resurrect, continuum) |
| **Редактор** | Neovim stable (vim-plug, Telescope, Treesitter, Dracula) |
| **Утилиты** | FZF, Ripgrep, fd, bat |
| **Runtime** | Node.js LTS |
| **Безопасность** | UFW, Fail2Ban, SSH hardening |

### Использование

```bash
wget -qO- https://raw.githubusercontent.com/energet666/helpers/main/setup.sh | bash
```

Или скачать и запустить вручную:

```bash
wget -O setup.sh https://raw.githubusercontent.com/energet666/helpers/main/setup.sh
cat setup.sh        # посмотреть содержимое
bash setup.sh       # запустить
```

### Требования

- **ОС:** Debian или Ubuntu
- **Архитектура:** x86_64 или aarch64
- Пользователь с доступом к `sudo`

### Особенности

- **Идемпотентность** — скрипт можно запускать повторно без побочных эффектов
- **Обработка ошибок** — при ошибке выводится номер строки и команда
- **SSH hardening** — отключение входа по паролю только при наличии ключей в `~/.ssh/authorized_keys`
- **Бэкапы** — конфиг SSH сохраняется с timestamp перед изменением
